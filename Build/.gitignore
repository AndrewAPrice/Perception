# Packages that are downloaded and not part of the Build source.
package-lock.json
node_modules

# Configuration file for the system that is running the build script.
config.json

# Temporary files we output during building.
temp/

# Temporary file used by Sublime Text.
perception.sublime-workspace